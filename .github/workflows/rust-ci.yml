name: Rust CI

on:
  push:
    branches: [main, develop]
    paths:
      - "apps/rust/**"
      - ".github/workflows/rust-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/rust/**"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Cache Nix store
        uses: actions/cache@v4
        with:
          path: /tmp/nix-cache
          key: ${{ runner.os }}-nix-rust-${{ hashFiles('flake.lock', 'ops/nix/environments/rust/shell.nix') }}
          restore-keys: |
            ${{ runner.os }}-nix-rust-

      - name: Check formatting
        run: nix develop .#rust --command bash -c "cd apps/rust && cargo fmt --check"

      - name: Run Clippy
        run: nix develop .#rust --command bash -c "cd apps/rust && cargo clippy -- -D warnings"

      - name: Check complexity
        run: nix develop .#rust --command bash -c "cd apps/rust && cargo clippy -- -D clippy::cognitive_complexity"

      - name: Run tests
        run: nix develop .#rust --command bash -c "cd apps/rust && cargo test"
